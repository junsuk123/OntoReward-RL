# The learner. Isaac Sim, PX4, ROS 2 Humble and the Micro XRCE-DDS agent are
# installed by scripts/bootstrap_*.sh and are not pip packages.
#
# torch: the R-GAT potential and the PPO networks. Install the CUDA build that
# matches the driver -- see https://pytorch.org/get-started/locally/ -- or the
# CPU build, which the whole pipeline still runs on (cfg.device.rgat='cpu').
torch>=2.1
numpy>=1.24,<3
matplotlib>=3.6
PyYAML>=6,<7
# Learner-side synthetic keypoint pretraining renders the deployed ArUco board.
opencv-python>=4.7,<5
